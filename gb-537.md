# gb-537 PKI 证书轮换影响推演

请生成一个名为 `pki-certificate-rollover-impact` 的 Go 全栈 Web 项目，面向企业 PKI 与平台安全团队，用于维护信任锚和证书链、登记依赖服务，并在离线环境推演根证书或中间证书轮换的影响。系统仅用于决策支持和仿真，不签发、撤销、部署或替换真实证书，不连接生产密钥系统；不得实现商城、电商、预约、订单、仓储库存、财务、客服工单、博客或大众社区。

## 1. 项目概述与复杂度下限

- 类型：B2B 前后端分离全栈 Web 应用。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 四实体必须贯穿 PostgreSQL 表、Go model/dto/repository/service/handler/router、React type/api/store/page。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件；证书字段、状态、算法版本和权限变化必须触达至少 5 个文件。

## 2. 核心实体

1. `TrustAnchor`（信任锚）
   - 字段：`id`、`anchor_code`、`subject_dn`、`serial_number`、`fingerprint_sha256`、`not_before`、`not_after`、`key_algorithm`、`certificate_state`、`pem_redacted`、`created_at`。
   - 关系与动作：作为多个证书链根节点并被轮换场景引用；支持导入公钥证书、指纹校验、标记生命周期和归档，禁止保存私钥。
   - 必须贯穿 `trust_anchors` 表 -> `model/trust_anchor.go` -> dto/repository/service/handler/router 独立文件 -> `frontend/src/types/trust-anchor.ts`、api、store、页面。
2. `CertificateChain`（证书链）
   - 字段：`id`、`chain_code`、`trust_anchor_id`、`leaf_subject`、`certificate_refs_json`、`chain_fingerprint`、`valid_from`、`valid_to`、`validation_result`、`chain_state`、`source_checksum`。
   - 关系与动作：引用信任锚并被多个依赖服务使用；支持导入公钥链、结构校验、版本对比和废止。
   - 必须贯穿 `certificate_chains` 表及独立后端分层、前端 type/api/store/page 文件。
3. `DependentService`（依赖服务）
   - 字段：`id`、`service_code`、`name`、`owner_team`、`environment`、`chain_id`、`client_trust_refs_json`、`protocol`、`criticality`、`dependency_edges_json`、`service_state`、`updated_at`。
   - 关系与动作：引用证书链并可依赖其他服务；支持登记、修改信任集合、维护依赖边和停用，不执行真实部署。
   - 必须贯穿 `dependent_services` 表及独立后端分层、前端 type/api/store/page 文件。
4. `RolloverScenario`（轮换场景）
   - 字段：`id`、`name`、`old_anchor_id`、`new_anchor_id`、`overlap_start`、`overlap_end`、`candidate_chain_ids`、`algorithm_version`、`input_hash`、`affected_services_json`、`broken_paths_json`、`scenario_state`、`explanation`、`created_by`、`verified_by`。
   - 关系与动作：冻结信任锚、证书链和服务依赖快照；支持草拟、仿真、比较、标记就绪、人工验证和回滚记录，禁止直控证书系统。
   - 必须贯穿 `rollover_scenarios` 表及独立后端分层、前端 type/api/store/page 文件。

## 3. 差异化算法与状态流

- 使用 Go 标准库 `crypto/x509` 解析脱敏公钥证书并验证签名链和有效期；严禁手写 ASN.1 字符串解析或接受私钥上传。
- 构建 `TrustAnchor -> CertificateChain -> DependentService -> downstream service` 依赖图，在轮换时间窗的关键时间点验证旧链、新链和交叠链，输出不可达服务、断裂路径、过早移除旧根和过晚引入新根等可解释原因。
- `CertificateState` 由日期与撤销标记计算为 `valid/expiring/expired/revoked`，不得由前端随意修改。
- `RolloverScenario`：`draft -> simulated -> ready -> executing -> verified`，`executing -> rollback`，`simulated/ready -> draft`；这里的 executing 仅表示演练步骤记录，不执行生产变更。非法迁移返回 409。
- 相同输入哈希、算法版本与模拟时间必须结果一致且幂等；保存逐路径验证证据。页面与 README 明确“不能替代生产 PKI 变更评审与双人复核”。

## 4. 核心页面与实体消费

1. `/anchors` 信任锚：消费 `TrustAnchor + CertificateChain`，展示指纹、有效期和链引用。
2. `/chains` 证书链：消费 `CertificateChain + TrustAnchor + DependentService`，查看链结构和离线验证结果。
3. `/dependencies` 服务依赖图：消费 `DependentService + CertificateChain`，编辑服务关系并检测循环依赖。
4. `/rollovers` 轮换推演：消费 `RolloverScenario + TrustAnchor + CertificateChain + DependentService`，选择时间窗、运行仿真并查看断裂路径。
5. `/audit` 审计中心：消费四实体变更投影和审计日志，按 request ID、实体、操作者和时间筛选。

`CertificateStateBadge` 被锚点、证书链和推演页共用；`DependencyGraph` 被依赖和推演页共用；`ValidationEvidenceDrawer` 被证书链、推演和审计页共用。

## 5. 横切关注点

- JWT + RBAC：角色至少为 `admin`、`pki_operator`、`service_owner`、`security_reviewer`、`auditor`；联动角色表、JWT claims、Go auth/rbac、路由、React guard、按钮显隐和类型。场景创建者不能验证自己的推演。
- 审计、脱敏与追踪：所有写操作保存 request ID、操作者和前后快照；证书仅记录公钥材料/指纹，日志禁止输出 PEM 全文、密钥或 JWT；仿真记录输入哈希、时间点、算法版本和路径摘要。
- 全局错误：证书解析/链校验输入错误返回 422，状态或版本冲突返回 409，权限返回 403，前端统一处理且不泄露敏感数据。
- 限流与幂等：登录、证书导入和推演接口分别限流；推演以 `Idempotency-Key` 防重。

## 6. 共享枚举、组件、hooks 与中间件

- `CertificateState = valid | expiring | expired | revoked`：后端 `backend/internal/constants/certificate_state.go`，前端 `frontend/src/types/enums/certificate-state.ts`。
- `ScenarioState = draft | simulated | ready | executing | verified | rollback`：后端 `backend/internal/constants/scenario_state.go`，前端 `frontend/src/types/enums/scenario-state.ts`。
- README 列出枚举在 model、dto、算法、service 状态机、store、组件、页面和测试中的所有位置。
- 共享组件：`CertificateStateBadge`、`DependencyGraph`、`ValidationEvidenceDrawer`；hooks：`useAuth`、`useRolloverSimulation`。
- 后端至少独立实现 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go`，并有独立脱敏 logger util。

## 7. 技术、规模与文件结构

- 英文短名：`pki-certificate-rollover-impact`。
- 前端：React 18 + TypeScript + Vite + Material UI + Zustand；后端：Go 1.22 + Gin + GORM；正式数据库 PostgreSQL 16，runtime smoke 支持 SQLite；JWT。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码 2500-4200 行、24-42 个功能 `.go` 文件且少于 5000 行；不计 `_test.go`、生成代码和 vendor。X.509 验证、图遍历、时间窗、状态机、脱敏和权限须有测试。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根 `go.work` 纳入 `./backend`，支持根执行 `go work sync`、`go build ./backend/...`、`go test ./backend/...`。

```text
.
├── go.work  runtime_smoke.json
├── docker-compose.yml  .env  .env.example  .gitignore  README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod  Dockerfile  cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ x509util/ util/
├── frontend/
│   ├── package.json  Dockerfile  nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四实体必须在所有后端层和前端 api/store/type 中按实体分文件。严禁合并职责到单一文件，禁止单个通用 CRUD 文件承载全部实体。

## 8. 运行 Manifest、部署与验证交付

- 固定端口：前端 `18537`、后端 `19537`、PostgreSQL 宿主机 `57537`；容器内 `80/8080/5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20537",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20537/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端必须支持上述 SQLite smoke 配置并自动建表/种子数据；正式 Compose 仍使用 PostgreSQL。该 manifest 的字段仅限示例中列出的启动字段。
- Compose 顶层 `name: pki-certificate-rollover-impact` 且无 `version:`；编排 db/backend/frontend，使用命名卷和英文短名容器名前缀，数据库、后端和前端均有 healthcheck，并按健康条件依赖。
- `.env`、`.env.example` 均含 `COMPOSE_PROJECT_NAME=pki-certificate-rollover-impact`、`FRONTEND_PORT=18537`、`BACKEND_PORT=19537`、`DB_PORT=57537`、`DB_DRIVER=postgres`、DB 变量和 `JWT_SECRET`；`.env` 不提交。前端只访问 `/api`，Nginx 原样代理 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz` 并支持 SPA。
- README 首屏给 `docker compose up -d`，说明项目介绍、主要功能、角色账号、架构与目录、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、算法与安全边界、runtime smoke、构建测试、常见问题、停止方式和 License；README 不得承载执行证据。
- 必须真实执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`；前后端均须有 healthcheck，所有长驻服务必须 healthy。
- 至少 8 个真实 API 冒烟：health、登录、四实体查询、锚点/依赖创建、合法/非法状态迁移、推演运行/读取、403 权限，覆盖成功与失败分支。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 只用 Codex 内置 Browser 验证登录、锚点、证书链、依赖图、轮换推演五页，禁止外部 Chrome；实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡。
- 所有构建、测试、runtime smoke、Compose、至少 8 条 API 的 method/path/expected/actual/assertion、Browser 页面、console/network、关闭结果等执行证据，只能写入 `output/execution.md`。截图必须直接放在 `output/`，至少有桌面和移动端两张，并在 execution.md 引用；禁止在其他 JSON/Markdown 中复制执行证据或伪造通过。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 验证后执行 `docker compose down -v --remove-orphans`，确认容器、网络和命名卷均已清理，同时停止 runtime smoke 进程。
- 项目必须独立 `git init`，设置本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后提交 `git add -A && git commit -m "feat: implement and verify pki-certificate-rollover-impact"`，把实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 为空，不得共用 Git 仓库。
